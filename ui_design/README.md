# Codebase Analyzer UI

Web app source for the Codebase Analyzer demo.

## Run

```bash
cd ui_design
npm install
npm run dev
```

The app can analyze:
- the bundled repository snapshot from this workspace
- a local folder chosen in the browser
- a folder uploaded with the fallback input
